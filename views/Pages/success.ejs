<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login With Google</title>
</head>
<style>
    body {
        display: flex;
        justify-content: flex-start;
        padding-top: 50px;
        flex-direction: column;
        align-items: center;
        background: #171717;
        position: absolute;
        top: 0;
        bottom: 0;
        right: 0;
        left: 0;
    }
    .container {
        margin-top: 10px;
        background: #cecece;
        padding: 10px;
        width: 50%;
        border-radius: 5px;
    }
    .box {
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: space-around;
    }
    button {
        padding: 10px;
        font-size: 18px;
        background: rgb(154, 16, 6);
        border: 1px solid #cecece4e;
        cursor: pointer;
        color: #fff;
    }
    .name {
        width: 60%;
        padding: 10px;
        background: #4343431e;
        border: 1px solid #000000a7;
        color: #000000;
        text-align: center;
    }
    .email {
        text-align: center;
        width: 60%;
        padding: 10px;
        background: rgb(195, 195, 195);
        border: 1px solid #000000a7;

    }
    #pop-up {
        position: absolute;
        top: 50px;
        right: 45%;
        padding: 10px;
        background: rgb(141, 126, 126);
        border: 1px solid #000000a7;
        color: #000000;
    }
    .box .logout {
        margin: 10px 0;
        color: #ffffff;
        background: #ba0000;
        padding: 10px;
        position: absolute;
        right: 0;
        top: 0;
    }
    .box .toform {
        margin: 10px 0;
        color: #000000;
        background: #cecece;
        padding: 10px;
        position: absolute;
        right: 100px;
        top: 0;
    }
</style>
<body>
    <h1 style="color: #fff;">Welcome</h1>
    <div class="container">
        <div class="box">
            <p class="name">
                <%= user.name.familyName  %> <%= user.name.givenName  %></p>
            <div class="dp">
                <img src="<%= user.picture  %>" alt="">
            </div>
            <p class="email"><%= user.email  %></p>
            <% if(
                user.email == "eddymugisha65@gmail.com" 
            || user.email == "princeineza@gmail.com" 
            || user.email == "nduwumwepacific@gmail.com"
            ) { %>
            <a href="/send-letter" class="toform">Send Email</a>
            <% } %>
            <a href="/logout" class="logout">Log Out</a>
            <form action="/add/?email=<%=user.email%>&name=<%=user.name.givenName%>" method="POST" Content-Type="application/x-www-form-urlencoded">
                <button>Subscribe to Our News Letter.</button>
            </form>
        </div>
    </div>
</body>
</html>